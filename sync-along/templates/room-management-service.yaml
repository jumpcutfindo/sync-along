apiVersion: v1
kind: Service
metadata:
  name: {{ index .Values "room-management-component" "service" "name" }}
  labels:
    {{- include "sync-along.labels" . | nindent 4 }}
spec:
  type: {{ index .Values "room-management-component" "service" "type" }}
  ports:
    - port: {{ index .Values "room-management-component" "service" "port" }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sync-along.selectorLabels" . | nindent 4 }}
